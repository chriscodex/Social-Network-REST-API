version: "3.8"

services:
  api:
    build: .
    container_name: api

    ports:
      - "5050:5050"

    environment:
      DATABASE_URL: postgres://postgres:postgres@db:5432/postgres?sslmode=disable

    depends_on:
      - db

  database:
    build: ./database
    container_name: database
